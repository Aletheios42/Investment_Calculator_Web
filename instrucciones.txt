- **Documentación de Tareas:**
  Se ha creado el archivo `instruciones.txt` para actualizar con los detalles de cada commit.

**Commit Header:
- crea el componente "header" con:
  ``` shell
  ng g c header --skip-tests
  ```
  - en header.component.html crea un header con un heading y una imagen(src = investment-calculator.png)
  - en header.component.css, simplemente copia el archivo en tu proyecto
  - importa la clase HeaderComponent en app.component.ts, y usa "<app-header />" en app.component.html
