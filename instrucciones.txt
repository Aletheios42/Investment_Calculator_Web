- **Documentación de Tareas:**
  Se ha creado el archivo `instruciones.txt` para actualizar con los detalles de cada commit.

**Commit User-Input part II**: el objetivo es recoger el input del usuario gracias a un formilario.

- importa la clase FormsModule en user-input.component.ts para poder acceder a eventos custom de angular
- añade un evento (ngSubmit)= al formulario que hemos creado en el commit anterior
- crea un metodo onSubmit() que muestro por pantalla "Submitted"

una vez completado deberias ver en la consola del navegador: "Submitted"
